export class BatchClose{
  academicSessionId?: number;
  programId?: number;
  operationalVerticalId?: number;
  isClosed?: boolean;
  closedDate?: Date;
  closedBy?: string;
  closedReason?: string;
  batchCodes?: string[]
}